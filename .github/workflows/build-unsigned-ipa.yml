name: Build Unsigned IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    name: Build unsigned iOS app
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.4.app

      - name: Resolve Swift packages
        run: |
          xcodebuild \
            -resolvePackageDependencies \
            -project CodexMobile/CodexMobile.xcodeproj \
            -scheme CodexMobile

      - name: Archive without signing
        run: |
          set -euo pipefail
          xcodebuild archive \
            -project CodexMobile/CodexMobile.xcodeproj \
            -scheme CodexMobile \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$RUNNER_TEMP/CodexAnywhere.xcarchive" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            DEVELOPMENT_TEAM=""

      - name: Package unsigned IPA
        run: |
          set -euo pipefail
          APP_PATH="$RUNNER_TEMP/CodexAnywhere.xcarchive/Products/Applications/CodexMobile.app"
          IPA_ROOT="$RUNNER_TEMP/ipa"
          IPA_PATH="$RUNNER_TEMP/CodexAnywhere-unsigned.ipa"

          test -d "$APP_PATH"
          rm -rf "$IPA_ROOT" "$IPA_PATH"
          mkdir -p "$IPA_ROOT/Payload"
          cp -R "$APP_PATH" "$IPA_ROOT/Payload/Codex Anywhere.app"

          cd "$IPA_ROOT"
          /usr/bin/zip -qry "$IPA_PATH" Payload

          mkdir -p "$GITHUB_WORKSPACE/build"
          cp "$IPA_PATH" "$GITHUB_WORKSPACE/build/CodexAnywhere-unsigned.ipa"

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: CodexAnywhere-unsigned-ipa
          path: build/CodexAnywhere-unsigned.ipa
          if-no-files-found: error
